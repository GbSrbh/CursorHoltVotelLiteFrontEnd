# Backend API base URL (domain only, no trailing slash).
# When set, all /api/* requests go to this domain instead of the same origin.
# Example: https://your-backend.vercel.app or http://localhost:4000
# Leave empty to use same origin + /api (dev proxy or same-host backend).
VITE_API_URL=
